a=1 # a라는 메모리 공간을 만든다. 1을 값으로 넣는다 
b=22 # b라는 메모리 공간을 만들고, 22를 값으로 넣는다
c=a+b # a와 b를 더한 값을 c라는 메모리 공간에 넣는다
print("a와 b를 더한 값:", c) #print : 출력

import tensorflow as tf # import : 포함시키다 / as tf 줄임말
print("텐서플로 버전 : ", tf.__version__)

# ctrl +F5 : 실행
# ctrl +Z : 취소
# ctrl +C,V : 한 라인 복붙
# shift + Delete : 한 라인 삭제 